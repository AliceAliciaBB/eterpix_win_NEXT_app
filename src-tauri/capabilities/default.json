{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for EterPix VRC Uploader",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
